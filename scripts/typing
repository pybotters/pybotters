#!/bin/bash -eux

# Run type checking with mypy.

uv run \
    --with-editable . \
    --with-requirements requirements/dev-typing.txt \
    --with-requirements requirements/dev-test.txt \
    --isolated \
    --no-project \
    mypy
